name: Transfer API Authority RED
on:
  push:
    branches:
      - refactor/background-downloader-authority
    paths:
      - test/core/services/background_downloader_transfer_api_authority_test.dart
permissions:
  contents: read
jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: refactor/background-downloader-authority
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1
          cache: true
      - run: flutter pub get
      - run: flutter test test/core/services/background_downloader_transfer_api_authority_test.dart
