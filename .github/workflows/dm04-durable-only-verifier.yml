name: DM-04 recovery verifier

on:
  push:
    branches: [plan/download-manager-reliability]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: dm04-recovery-${{ github.ref }}
  cancel-in-progress: true

jobs:
  recovery-policy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1
          cache: true
      - name: Install dependencies and generate sources
        run: |
          flutter pub get
          flutter gen-l10n
          dart run build_runner build --delete-conflicting-outputs
      - name: Run DM-04 focused recovery tests
        run: >-
          flutter test
          test/core/services/download_durable_only_recovery_test.dart
          test/core/services/download_missing_presentation_recovery_test.dart
          --dart-define=ANIMEWITCHER_FIREBASE_API_KEY=test-api-key
