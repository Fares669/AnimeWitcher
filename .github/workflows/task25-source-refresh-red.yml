name: Task 25 Source Refresh RED
on:
  push:
    branches:
      - refactor/background-downloader-authority
    paths:
      - test/core/services/download_plugin_source_refresh_routing_test.dart
permissions:
  contents: read
jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: refactor/background-downloader-authority
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1
          cache: true
      - run: flutter pub get
      - run: flutter test test/core/services/download_plugin_source_refresh_routing_test.dart
